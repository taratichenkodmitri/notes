import NoteButton from '../NoteButton/NoteButton';
import './NoteAddButton.css';

const NoteAddButton = () => {
  return <NoteButton additionalClass="note-add">New note</NoteButton>;
};

export default NoteAddButton;
